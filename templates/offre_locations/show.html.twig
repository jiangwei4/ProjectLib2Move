{% extends 'base.html.twig' %}

{% block title %}OffreLocations{% endblock %}

{% block body %}
    <h1>OffreLocations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre_location.id }}</td>
            </tr>
            <tr>
                <th>KmMax</th>
                <td>{{ offre_location.KmMax }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ offre_location.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_locations_index') }}">back to list</a>

    <a href="{{ path('offre_locations_edit', {'id': offre_location.id}) }}"><img src="\img\edit.png"></a></a>

    {{ include('offre_locations/_delete_form.html.twig') }}
{% endblock %}
