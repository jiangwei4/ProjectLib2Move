{% extends 'base.html.twig' %}

{% block title %}Gamme{% endblock %}

{% block body %}
    <h1>Gamme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gamme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ gamme.Nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gamme_index') }}">back to list</a>

    <a href="{{ path('gamme_edit', {'id': gamme.id}) }}">edit</a>

    {{ include('gamme/_delete_form.html.twig') }}
{% endblock %}
